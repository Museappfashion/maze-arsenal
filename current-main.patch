diff --git a/src/services/leaderboard.js b/src/services/leaderboard.js
--- a/src/services/leaderboard.js
+++ b/src/services/leaderboard.js
@@
 export const LEADERBOARD_LIMIT = 10;

-export const GLOBAL_LEADERBOARD_TABLE = "leaderboard_scores";
 export const GLOBAL_LEADERBOARD_RPC = "get_global_leaderboard";
+export const START_LEADERBOARD_RUN_RPC = "start_leaderboard_run";
+export const FINISH_LEADERBOARD_RUN_RPC = "finish_leaderboard_run";
@@
 export function normalizeLeaderboardEntries(entries) {
@@
     .sort((a, b) => a.time - b.time)
     .slice(0, LEADERBOARD_LIMIT);
 }
+
+export function normalizePersonalBestEntries(entries) {
+  return normalizeLeaderboardEntries(entries).slice(0, 1);
+}
+
 export function normalizeLevelLeaderboards(levelBoards) {
   if (Array.isArray(levelBoards)) {
     return {
-      "2d": normalizeLeaderboardEntries(levelBoards),
+      "2d": normalizePersonalBestEntries(levelBoards),
       "3d": [],
     };
   }

   return {
-    "2d": normalizeLeaderboardEntries(levelBoards?.["2d"]),
-    "3d": normalizeLeaderboardEntries(levelBoards?.["3d"]),
+    "2d": normalizePersonalBestEntries(levelBoards?.["2d"]),
+    "3d": normalizePersonalBestEntries(levelBoards?.["3d"]),
   };
 }
@@
   const normalizedMode = mode === "3d" ? "3d" : "2d";
   const levelBoards = normalizeLevelLeaderboards(leaderboards[levelKey]);
-  const nextEntries = normalizeLeaderboardEntries([
+  const nextEntries = normalizePersonalBestEntries([
     ...levelBoards[normalizedMode],
     {
       time,
@@
   return data.session;
 }
+
+export async function startGlobalLeaderboardRun(levelKey, mode) {
+  if (
+    !supabase ||
+    !LEVELS[levelKey] ||
+    LEVELS[levelKey].leaderboard === false
+  ) {
+    return null;
+  }
+
+  await ensureGlobalLeaderboardSession();
+
+  const { data, error } = await supabase.rpc(
+    START_LEADERBOARD_RUN_RPC,
+    {
+      p_level_key: levelKey,
+      p_mode: mode === "3d" ? "3d" : "2d",
+    },
+  );
+
+  if (error) {
+    throw error;
+  }
+
+  return typeof data === "string" && data ? data : null;
+}
+
+export async function finishGlobalLeaderboardRun(
+  runId,
+  playerName,
+  countryCode,
+) {
+  if (!supabase || !runId) {
+    return null;
+  }
+
+  await ensureGlobalLeaderboardSession();
+
+  const { data, error } = await supabase.rpc(
+    FINISH_LEADERBOARD_RUN_RPC,
+    {
+      p_run_id: runId,
+      p_player_name: getPlayerDisplayName(playerName),
+      p_country_code: normalizeCountryCode(countryCode) || null,
+    },
+  );
+
+  if (error) {
+    throw error;
+  }
+
+  return {
+    improved: Boolean(data?.improved),
+    timeSeconds: Number(data?.timeSeconds) || null,
+  };
+}
@@
-export async function submitGlobalLeaderboardTime(
-  levelKey,
-  mode,
-  time,
-  playerName,
-  countryCode,
-) {
-  if (
-    !supabase ||
-    !LEVELS[levelKey] ||
-    LEVELS[levelKey].leaderboard === false ||
-    !Number.isFinite(time) ||
-    time <= 0
-  ) {
-    return false;
-  }
-
-  const session = await ensureGlobalLeaderboardSession();
-  const normalizedMode = mode === "3d" ? "3d" : "2d";
-  const roundedTime = Math.round(time * 1000) / 1000;
-  const { error } = await supabase.from(GLOBAL_LEADERBOARD_TABLE).insert({
-    user_id: session.user.id,
-    player_name: getPlayerDisplayName(playerName),
-    level_key: levelKey,
-    mode: normalizedMode,
-    time_seconds: roundedTime,
-    country_code: normalizeCountryCode(countryCode) || null,
-  });
-
-  if (error) {
-    throw error;
-  }
-
-  return true;
-}
diff --git a/src/game/world.js b/src/game/world.js
--- a/src/game/world.js
+++ b/src/game/world.js
@@
 import { CANVAS_HEIGHT, CANVAS_WIDTH, DEFAULT_LEVEL_KEY, LEVELS } from "../config/constants.js";
 import { normalizeLabyrinthOptions } from "../config/labyrinth.js";
+import { applyRobbienatorLoadout } from "../config/robbienator.js";
 import { WEAPON_HOTKEY_LABEL } from "../config/weapons.js";
@@
     viewMode: normalizedViewMode,
     runMode: normalizedViewMode,
+    leaderboardEligible: !labyrinthMode,
@@
     placeProgressionItems(world, distancesFromStart, used);
     placePowerUps(world, distancesFromStart, used);
     placeEnemies(world, distancesFromStart, used);
   }
+  applyRobbienatorLoadout(world);
   updateVisionCache(world);
diff --git a/src/config/presentations.js b/src/config/presentations.js
--- a/src/config/presentations.js
+++ b/src/config/presentations.js
@@
 import { LEVEL_THEMES } from "./constants.js";
 import { POWER_UPS } from "./powerUps.js";
+import {
+  hasRobbienatorLoadout,
+  isRobbienatorWeapon,
+  ROBBIENATOR_LABEL,
+} from "./robbienator.js";
 import { WEAPONS } from "./weapons.js";
@@
 export function getWeaponPresentation(world, weaponKey) {
+  if (isRobbienatorWeapon(world, weaponKey)) {
+    return {
+      label: ROBBIENATOR_LABEL,
+      description:
+        "A suspiciously powerful precision banana with excellent potassium.",
+    };
+  }
+
   return (
@@
 export function getAmmoLabel(world) {
+  if (hasRobbienatorLoadout(world)) {
+    return "Ammo";
+  }
   return isMedievalTheme(world) ? "Arrows" : "Ammo";
 }

 export function getAmmoPickupLabel(world) {
+  if (hasRobbienatorLoadout(world)) {
+    return "Ammo";
+  }
   return isMedievalTheme(world) ? "Arrow Bundle" : "Ammo";
 }

 export function getAmmoMessageLabel(world) {
+  if (hasRobbienatorLoadout(world)) {
+    return "ammo";
+  }
   return isMedievalTheme(world) ? "arrows" : "ammo";
 }
diff --git a/src/game/labyrinth.js b/src/game/labyrinth.js
--- a/src/game/labyrinth.js
+++ b/src/game/labyrinth.js
@@
 function buildPickupCandidates(world, distances, used) {
-  const exitDistance = Math.max(
-    1,
-    distances[indexOfTile(world.width, world.exit.x, world.exit.y)],
-  );
+  let maxReachableDistance = 1;
+
+  for (const tile of world.floorTiles) {
+    const distance =
+      distances[indexOfTile(world.width, tile.x, tile.y)];
+
+    if (distance > maxReachableDistance) {
+      maxReachableDistance = distance;
+    }
+  }
+
   const candidates = world.floorTiles
     .filter((tile) => {
       const key = indexOfTile(world.width, tile.x, tile.y);
       const distance = distances[key];
       return (
         !used.has(key) &&
-        distance > 8 &&
+        distance > 3 &&
         distance >= 0 &&
-        distance < exitDistance * 0.98
+        distance <= maxReachableDistance
       );
     })
@@
 function chooseProgressionTile(candidates, index, count, used, world) {
   if (!candidates.length) {
     return null;
   }
-  const progress = (index + 1) / (count + 1);
+  const progress = count <= 1 ? 0.5 : (index + 0.5) / count;
   const targetIndex = Math.min(
     candidates.length - 1,
     Math.floor(progress * candidates.length),
   );
-  const spread = Math.max(3, Math.floor(candidates.length / Math.max(4, count)));
-  const jitter = Math.floor((Math.random() - 0.5) * spread * 2);
+  const spread = Math.max(
+    2,
+    Math.floor(candidates.length / Math.max(6, count * 2)),
+  );
+  const jitter = Math.floor((Math.random() - 0.5) * spread);
@@
 function placeLabyrinthPickups(
   world,
   distances,
   breakerCount,
   lightKeys,
 ) {
@@
   const used = new Set([
     indexOfTile(world.width, playerTileX, playerTileY),
     indexOfTile(world.width, world.exit.x, world.exit.y),
   ]);
-  placeLabyrinthLights(world, distances, used, lightKeys);
-  placeLabyrinthBreakers(world, distances, used, breakerCount);
+
+  const validLightKeys = lightKeys.filter(
+    (key) => LABYRINTH_LIGHTS[key],
+  );
+  const normalizedBreakerCount = Math.max(
+    0,
+    Math.round(breakerCount),
+  );
+  const candidates = buildPickupCandidates(
+    world,
+    distances,
+    used,
+  );
+  const plan = [];
+  let lightIndex = 0;
+  let breakerIndex = 0;
+
+  while (
+    lightIndex < validLightKeys.length ||
+    breakerIndex < normalizedBreakerCount
+  ) {
+    const lightProgress =
+      validLightKeys.length > 0
+        ? lightIndex / validLightKeys.length
+        : Infinity;
+    const breakerProgress =
+      normalizedBreakerCount > 0
+        ? breakerIndex / normalizedBreakerCount
+        : Infinity;
+
+    if (
+      lightIndex < validLightKeys.length &&
+      (
+        breakerIndex >= normalizedBreakerCount ||
+        lightProgress <= breakerProgress
+      )
+    ) {
+      plan.push({
+        type: "light",
+        lightKey: validLightKeys[lightIndex],
+      });
+      lightIndex += 1;
+    } else {
+      plan.push({ type: "breaker" });
+      breakerIndex += 1;
+    }
+  }
+
+  for (let index = 0; index < plan.length; index += 1) {
+    const tile = chooseProgressionTile(
+      candidates,
+      index,
+      plan.length,
+      used,
+      world,
+    );
+
+    if (!tile) {
+      break;
+    }
+
+    const center = tileCenter(tile);
+    const item = plan[index];
+
+    if (item.type === "light") {
+      const light = LABYRINTH_LIGHTS[item.lightKey];
+
+      world.pickups.push({
+        id: `labyrinth-light-${item.lightKey}-${world.nextId++}`,
+        type: "labyrinthLight",
+        lightKey: item.lightKey,
+        label: light.label,
+        x: center.x,
+        y: center.y,
+        radius: 0.28,
+        color: light.color,
+      });
+      continue;
+    }
+
+    world.pickups.push({
+      id: `labyrinth-breaker-${world.nextId++}`,
+      type: "labyrinthBreaker",
+      label: "10s Wall Breaker",
+      x: center.x,
+      y: center.y,
+      radius: 0.28,
+      color: "#c084fc",
+    });
+  }
 }
diff --git a/src/audio/MazeAudioEngine.js b/src/audio/MazeAudioEngine.js
--- a/src/audio/MazeAudioEngine.js
+++ b/src/audio/MazeAudioEngine.js
@@
       case "labyrinthShift":
         this.noise(now, 0.34, 0.11, this.sfxGain, 420);
         this.tone(82.41, now, 0.46, 0.1, "sawtooth", this.sfxGain);
         this.tone(58.27, now + 0.08, 0.52, 0.08, "triangle", this.sfxGain);
         break;
+      case "labyrinthTick": {
+        if (!this.canPlaySfx("labyrinthTick", 0.14)) {
+          break;
+        }
+
+        const critical = Boolean(event.critical);
+        const urgent = Boolean(event.urgent);
+        const frequency = critical ? 1180 : urgent ? 940 : 720;
+        const volume = critical ? 0.07 : urgent ? 0.052 : 0.036;
+
+        this.tone(
+          frequency,
+          now,
+          critical ? 0.055 : 0.04,
+          volume,
+          "square",
+          this.sfxGain,
+        );
+
+        if (critical) {
+          this.tone(
+            frequency * 0.62,
+            now + 0.048,
+            0.055,
+            0.038,
+            "triangle",
+            this.sfxGain,
+          );
+        }
+        break;
+      }
       case "powerUpUse":
diff --git a/src/game/rendering.js b/src/game/rendering.js
--- a/src/game/rendering.js
+++ b/src/game/rendering.js
@@
 import { POWER_UPS } from "../config/powerUps.js";
 import { getAmmoLabel, getTheme, getWeaponLabel, isMedievalTheme } from "../config/presentations.js";
+import { isRobbienatorWeapon } from "../config/robbienator.js";
 import { WORLD_LABEL_BG, WORLD_LABEL_BORDER, WORLD_LABEL_FONT, WORLD_LABEL_TEXT } from "../config/runtime.js";
@@
 export function drawMedievalBowShape(ctx, weaponKey, size, wood, metal, glow) {
@@
 }
+
+export function drawRobbienatorShape(ctx, size) {
+  ctx.save();
+  ctx.rotate(-0.12);
+
+  const banana = ctx.createLinearGradient(
+    -size * 0.5,
+    -size * 0.35,
+    size * 0.8,
+    size * 0.35,
+  );
+  banana.addColorStop(0, "#ca8a04");
+  banana.addColorStop(0.24, "#fde047");
+  banana.addColorStop(0.58, "#facc15");
+  banana.addColorStop(1, "#eab308");
+
+  ctx.fillStyle = banana;
+  ctx.strokeStyle = "#713f12";
+  ctx.lineWidth = Math.max(1.5, size * 0.055);
+  ctx.beginPath();
+  ctx.moveTo(-size * 0.48, -size * 0.18);
+  ctx.bezierCurveTo(
+    -size * 0.18,
+    size * 0.34,
+    size * 0.48,
+    size * 0.42,
+    size * 0.9,
+    -size * 0.08,
+  );
+  ctx.bezierCurveTo(
+    size * 0.46,
+    size * 0.14,
+    -size * 0.08,
+    size * 0.08,
+    -size * 0.34,
+    -size * 0.32,
+  );
+  ctx.closePath();
+  ctx.fill();
+  ctx.stroke();
+
+  ctx.fillStyle = "#422006";
+  const faceX = size * 0.22;
+  const faceY = size * 0.06;
+
+  for (const eyeX of [faceX - size * 0.1, faceX + size * 0.1]) {
+    ctx.beginPath();
+    ctx.arc(
+      eyeX,
+      faceY - size * 0.1,
+      Math.max(1.5, size * 0.038),
+      0,
+      Math.PI * 2,
+    );
+    ctx.fill();
+  }
+
+  ctx.strokeStyle = "#422006";
+  ctx.lineWidth = Math.max(1.5, size * 0.035);
+  ctx.lineCap = "round";
+  ctx.beginPath();
+  ctx.arc(
+    faceX,
+    faceY - size * 0.01,
+    size * 0.14,
+    0.15,
+    Math.PI - 0.15,
+  );
+  ctx.stroke();
+  ctx.restore();
+}

 export function drawWeaponShape(ctx, world, weaponKey, size) {
@@
-  if (weaponKey === "fists") {
+  if (isRobbienatorWeapon(world, weaponKey)) {
+    drawRobbienatorShape(ctx, size);
+  } else if (weaponKey === "fists") {
@@
 export function draw3DLabyrinthLight(ctx, world) {
@@
 }
+
+export function draw3DRobbienator(ctx, recoil) {
+  ctx.save();
+  ctx.translate(0, -76 - recoil * 8);
+  ctx.rotate(-0.13 - recoil * 0.04);
+
+  const width = 270;
+  const height = 155;
+  const banana = ctx.createLinearGradient(
+    -width * 0.55,
+    -height,
+    width * 0.55,
+    0,
+  );
+  banana.addColorStop(0, "#ca8a04");
+  banana.addColorStop(0.2, "#fde047");
+  banana.addColorStop(0.58, "#facc15");
+  banana.addColorStop(1, "#eab308");
+
+  ctx.shadowBlur = 24;
+  ctx.shadowColor = "rgba(250,204,21,0.45)";
+  ctx.fillStyle = banana;
+  ctx.strokeStyle = "#713f12";
+  ctx.lineWidth = 6;
+  ctx.beginPath();
+  ctx.moveTo(-width * 0.52, -height * 0.62);
+  ctx.bezierCurveTo(
+    -width * 0.18,
+    height * 0.06,
+    width * 0.35,
+    height * 0.12,
+    width * 0.56,
+    -height * 0.58,
+  );
+  ctx.bezierCurveTo(
+    width * 0.21,
+    -height * 0.22,
+    -width * 0.13,
+    -height * 0.22,
+    -width * 0.39,
+    -height * 0.82,
+  );
+  ctx.closePath();
+  ctx.fill();
+  ctx.stroke();
+
+  ctx.shadowBlur = 0;
+  ctx.fillStyle = "#422006";
+  const faceX = width * 0.08;
+  const faceY = -height * 0.38;
+
+  for (const eyeX of [faceX - 22, faceX + 22]) {
+    ctx.beginPath();
+    ctx.arc(eyeX, faceY - 19, 7, 0, Math.PI * 2);
+    ctx.fill();
+  }
+
+  ctx.strokeStyle = "#422006";
+  ctx.lineWidth = 7;
+  ctx.lineCap = "round";
+  ctx.beginPath();
+  ctx.arc(faceX, faceY - 2, 33, 0.18, Math.PI - 0.18);
+  ctx.stroke();
+  ctx.restore();
+}

 export function draw3DWeapon(ctx, world) {
@@
   ctx.save();
   ctx.translate(centerX, baseY);
-  if (weapon.type === "melee") {
+  if (isRobbienatorWeapon(world, world.player.weapon)) {
+    draw3DRobbienator(ctx, recoil);
+  } else if (weapon.type === "melee") {
diff --git a/src/App.jsx b/src/App.jsx
--- a/src/App.jsx
+++ b/src/App.jsx
@@
 import { GAME_STYLES } from "./styles/gameStyles.js";
 import { MazeAudioEngine, queueSfx } from "./audio/MazeAudioEngine.js";
 import { LabyrinthStatusPanel, MinimapPanel, MobileHudOverlay, SettingsControls, SidebarSettings, TouchControls, mergeInputKeys, selectNextOwnedWeapon } from "./components/GameUi.jsx";
+import { LabyrinthTimerOverlay } from "./components/LabyrinthTimerOverlay.jsx";
 import { LevelSelectScreen } from "./components/LevelSelectScreen.jsx";
+import { RunEndOverlay } from "./components/RunEndOverlay.jsx";
 import { StatCard } from "./components/StatCard.jsx";
+import { ThreeDPowerUpOverlay } from "./components/ThreeDPowerUpOverlay.jsx";
@@
-import { GLOBAL_LEADERBOARD_ENABLED, addLeaderboardTime, createEmptyUserRanks, detectCountryCode, fetchGlobalLeaderboards, loadLeaderboards, saveLeaderboards, submitGlobalLeaderboardTime } from "./services/leaderboard.js";
+import {
+  GLOBAL_LEADERBOARD_ENABLED,
+  addLeaderboardTime,
+  createEmptyUserRanks,
+  detectCountryCode,
+  fetchGlobalLeaderboards,
+  finishGlobalLeaderboardRun,
+  loadLeaderboards,
+  saveLeaderboards,
+  startGlobalLeaderboardRun,
+} from "./services/leaderboard.js";
@@
 const analyticsRunEndedRef = useRef(false);
 const audioRef = useRef(null);
+const leaderboardRunRef = useRef({
+  token: 0,
+  promise: null,
+});
+const [runGeneration, setRunGeneration] = useState(0);
@@
 const refreshGlobalLeaderboards = useCallback(async ({ silent = false } = {}) => {
@@
 }, []);
+
+const beginLeaderboardRun = useCallback((world) => {
+  const token = leaderboardRunRef.current.token + 1;
+
+  if (
+    !GLOBAL_LEADERBOARD_ENABLED ||
+    world.labyrinthMode ||
+    world.leaderboardEligible === false
+  ) {
+    leaderboardRunRef.current = {
+      token,
+      promise: null,
+    };
+    return;
+  }
+
+  const promise = startGlobalLeaderboardRun(
+    world.level.key,
+    world.runMode,
+  ).catch((error) => {
+    console.warn("Leaderboard run start failed:", error);
+    setLeaderboardStatus("offline");
+    return null;
+  });
+
+  leaderboardRunRef.current = {
+    token,
+    promise,
+  };
+}, []);
+
+const invalidateLeaderboardRun = useCallback((world) => {
+  world.leaderboardEligible = false;
+  leaderboardRunRef.current = {
+    token: leaderboardRunRef.current.token + 1,
+    promise: null,
+  };
+}, []);

 const recordLeaderboardScore = useCallback(
   (world) => {
-    if (world.labyrinthMode) {
+    if (
+      world.labyrinthMode ||
+      world.leaderboardEligible === false
+    ) {
       return;
     }
@@
-    if (!GLOBAL_LEADERBOARD_ENABLED) {
+    if (!GLOBAL_LEADERBOARD_ENABLED) {
       return;
     }

+    const runPromise = leaderboardRunRef.current.promise;
+    leaderboardRunRef.current = {
+      token: leaderboardRunRef.current.token + 1,
+      promise: null,
+    };
+
     void (async () => {
       try {
+        const runId = await runPromise;
+
+        if (!runId) {
+          return;
+        }
+
         const countryCode = await detectCountryCode();
-        await submitGlobalLeaderboardTime(
-          levelKey,
-          mode,
-          completedTime,
+        await finishGlobalLeaderboardRun(
+          runId,
           runPlayerName,
           countryCode,
         );
@@
   worldRef.current = nextWorld;
+  beginLeaderboardRun(nextWorld);
   startLevelAudio(nextWorld);
@@
-}, [clearTouchInput, forceRefresh, selectionMode, startLevelAudio]);
+}, [
+  beginLeaderboardRun,
+  clearTouchInput,
+  forceRefresh,
+  selectionMode,
+  startLevelAudio,
+]);
@@
   );
   worldRef.current = nextWorld;
+  beginLeaderboardRun(nextWorld);
   startLevelAudio(nextWorld);
@@
   analyticsRunEndedRef.current = false;
   void recordGameStarted(nextWorld);
+  setRunGeneration((generation) => generation + 1);
   forceRefresh();
 }, [
+  beginLeaderboardRun,
   clearTouchInput,
@@
 const switchGameMode = useCallback(() => {
   const nextViewMode = gameMode === "3d" ? "2d" : "3d";
+  const world = worldRef.current;

@@
-  setWorldViewMode(worldRef.current, nextViewMode);
+  if (!world.labyrinthMode) {
+    invalidateLeaderboardRun(world);
+    setMessage(
+      world,
+      "Leaderboard disabled for this run after switching view mode",
+      2.2,
+    );
+  }
+
+  setWorldViewMode(world, nextViewMode);
@@
-}, [clearTouchInput, forceRefresh, gameMode]);
+}, [
+  clearTouchInput,
+  forceRefresh,
+  gameMode,
+  invalidateLeaderboardRun,
+]);
@@
   drawWorld(ctx, world);
@@
-  frameRef.current = requestAnimationFrame(loop);
+  if (world.gameOver || world.victory) {
+    forceRefresh();
+    return;
+  }
+
+  frameRef.current = requestAnimationFrame(loop);
@@
   recordLeaderboardScore,
+  runGeneration,
   selectedLevel,
 ]);
@@
         <canvas
@@
         />
+        <LabyrinthTimerOverlay world={world} />
+        <ThreeDPowerUpOverlay
+          world={world}
+          storedPowerUps={storedPowerUps}
+          activePowerUps={activePowerUps}
+        />
+        <RunEndOverlay
+          world={world}
+          onRestart={resetWorld}
+          onMainMenu={returnToLevelSelect}
+        />
 {touchControlsEnabled && (
diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@
   "version": "1.1.0",
   "type": "module",
+  "engines": {
+    "node": ">=22.12.0"
+  },
   "scripts": {
